"use client";
import { useAuth } from "@/hooks/useAuth";
export default function LoginPage() { const { login, error } = useAuth(); return <div className="login"><section className="login-art"><p className="eyebrow" style={{color:"white"}}>Your quiet study space</p><h1>言葉<br/>ノート</h1><p>Build your Japanese vocabulary, one calm review at a time.</p></section><section className="login-form"><div className="login-box"><p className="eyebrow">Welcome to Kotoba</p><h2>Ready to study?</h2><p>Your vocabulary and learning progress stay private in your personal account.</p><button className="btn google" onClick={login}><b style={{color:"#4285f4",fontSize:19}}>G</b> Continue with Google</button>{error && <p className="error" role="alert">{error}</p>}<p className="hint">By continuing, you’ll sign in securely with Google. We only store your vocabulary and study preferences.</p></div></section></div>; }
